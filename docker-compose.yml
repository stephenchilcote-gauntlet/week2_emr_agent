services:
  mysql:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: openemr
      MYSQL_USER: openemr
      MYSQL_PASSWORD: openemr
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/seed_data.sql:/docker-entrypoint-initdb.d/99-seed.sql
    ports:
      - "3306:3306"
    networks:
      - emr-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 10

  openemr:
    build:
      context: .
      dockerfile: docker/Dockerfile.openemr
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      MYSQL_HOST: mysql
      MYSQL_ROOT_PASS: root
      MYSQL_USER: openemr
      MYSQL_PASS: openemr
      OE_USER: admin
      OE_PASS: pass
      MANUAL_SETUP: 0
      DEMO_MODE: standard
      OPENEMR_AGENT_API_URL: http://agent:8000
    volumes:
      - openemr_sites:/var/www/localhost/htdocs/openemr/sites
      - openemr_logs:/var/log
      - ./openemr/interface/modules/custom_modules/oe-module-clinical-assistant/public:/var/www/localhost/htdocs/openemr/interface/modules/custom_modules/oe-module-clinical-assistant/public
      - ./web/sidebar/sidebar.js:/var/www/localhost/htdocs/openemr/interface/modules/custom_modules/oe-module-clinical-assistant/public/assets/sidebar.js
      - ./web/sidebar/sidebar.css:/var/www/localhost/htdocs/openemr/interface/modules/custom_modules/oe-module-clinical-assistant/public/assets/sidebar.css
      - ./web/sidebar/overlay.js:/var/www/localhost/htdocs/openemr/interface/modules/custom_modules/oe-module-clinical-assistant/public/assets/overlay.js
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - emr-net

  agent:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      OPENEMR_BASE_URL: http://openemr:80
      OPENEMR_FHIR_URL: http://openemr:80/apis/default/fhir
      OPENEMR_CLIENT_ID: ${OPENEMR_CLIENT_ID}
      OPENEMR_CLIENT_SECRET: ${OPENEMR_CLIENT_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL:-claude-sonnet-4-20250514}
      OTEL_EXPORTER_OTLP_ENDPOINT: http://jaeger:4317
    volumes:
      - ./data:/app/data
      - ./web:/app/web
      - ./src:/app/src
    depends_on:
      - openemr
    networks:
      - emr-net

  jaeger:
    image: jaegertracing/jaeger:latest
    restart: unless-stopped
    ports:
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
    networks:
      - emr-net

networks:
  emr-net:
    driver: bridge

volumes:
  mysql_data:
  openemr_sites:
  openemr_logs:
